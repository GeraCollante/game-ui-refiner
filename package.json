{
  "name": "game-ui-refiner",
  "version": "0.1.0",
  "description": "VLM critic + generator loop for refining AI-generated game UIs into pixel-faithful Svelte components",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "node tools/check.mjs",
    "test": "node tests/run.mjs",
    "lint": "node tools/check.mjs && node tests/run.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/GeraCollante/game-ui-refiner"
  }
}
